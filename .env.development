# Ambiente de Desenvolvimento
NODE_ENV=development

# Banco de Dados Local (Desenvolvimento)
# WARNING: This points to the Neon production database. Use with EXTREME CAUTION in development.
# If you do not want to run against the production DB, replace this with your local Postgres connection.
LOCAL_DATABASE_URL=postgresql://<NEON_USER>:<NEON_PASSWORD>@<HOST>/<DB>?sslmode=require&channel_binding=require

# Sessão (gere um secret de 32 caracteres)
SESSION_SECRET=YOUR_SESSION_SECRET

# Backblaze Storage (S3-compatible / S2)
# Configurar com credenciais reais para habilitar upload para Backblaze S2
# Escolha do provider: 'b2' (legacy Backblaze B2 S3-compat) ou 's2' (Backblaze S2)
BACKBLAZE_PROVIDER=s2

# Endpoint/Região explicitamente configuráveis para S2
# Exemplo: BACKBLAZE_S2_ENDPOINT=https://s3.us-east-1.backblazeb2.com
BACKBLAZE_ENDPOINT=s3.us-east-005.backblazeb2.com
BACKBLAZE_REGION=US East

# Alternativamente, usar BACKBLAZE_ENDPOINT/BACKBLAZE_REGION (mantidos para compatibilidade)
BACKBLAZE_ENDPOINT=s3.us-east-005.backblazeb2.com
BACKBLAZE_REGION=US East

BACKBLAZE_BUCKET=laudos-qwork
BACKBLAZE_KEY_ID=2a8144c04121
BACKBLAZE_APPLICATION_KEY=0052a8144c041210000000001

# Debug (opcional)
DEBUG=false