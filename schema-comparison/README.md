# Schema Comparison — Snapshots

⚠️ **PRÉ-MIGRAÇÃO**: Estes dumps foram gerados ANTES das migrations 1016/1017.

Referências a `contratante`, `contratantes`, `contratante_id` etc. são nomes legados.
Após aplicação das migrations, os nomes corretos são `tomador`, `tomadores`, `tomador_id`.

Para gerar dumps atualizados, use `scripts/compare-databases-full.ts`.
