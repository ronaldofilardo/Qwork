// Script removido: este arquivo duplicava a geração do HTML do laudo.
// Use o gerador canonical `lib/templates/laudo-html.ts -> gerarHTMLLaudoCompleto()`
// e as APIs do emissor para geração/validação de PDFs.
// Mantido como stub para histórico e referência.

console.log(
  'scripts/checks/test-html-completo.cjs: script removido - use o gerador canonical.'
);
