// Script removido: este arquivo duplicava a geração de PDF do laudo.
// Use a API do emissor (`/api/emissor/laudos/:loteId/pdf`) ou o gerador canonical `lib/templates/laudo-html.ts -> gerarHTMLLaudoCompleto()`.

console.log(
  'scripts/checks/test-pdf-laudo.cjs: script removido - use fluxo do emissor.'
);
